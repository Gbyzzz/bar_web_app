import * as bytes from "bytes";

export class Image {
  imageId: number;
  // name: string;
  // filename: string;
  // contentType: string;
  // size: number;
  // bytes: Blob;


  constructor(imageId: number) {
    this.imageId = imageId;
  }
}
